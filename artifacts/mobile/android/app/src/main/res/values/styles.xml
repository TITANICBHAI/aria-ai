<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- AppTheme: used by Application tag + MainActivity (React Native host).
         Inherits from Theme.AppCompat.Light.NoActionBar — the standard base for
         React Native apps. windowSoftInputMode="adjustResize" is set in the
         manifest; this theme omits conflicting window flags. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:statusBarColor">@android:color/black</item>
        <item name="android:navigationBarColor">@android:color/black</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>

    <!-- Theme.ARIAAgent: used by ComposeMainActivity (Jetpack Compose UI).
         Inherits from Theme.MaterialComponents.DayNight.NoActionBar so
         Material3 components (Button, Card, etc.) render without a toolbar. -->
    <style name="Theme.ARIAAgent" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:statusBarColor">@android:color/black</item>
        <item name="android:navigationBarColor">@android:color/black</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:windowLightNavigationBar" tools:targetApi="27">false</item>
    </style>

</resources>
